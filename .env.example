# Alpaca (paper only — live keys will cause an immediate hard error at startup)
ALPACA_API_KEY=your_paper_api_key
ALPACA_SECRET_KEY=your_paper_secret_key
ALPACA_BASE_URL=https://paper-api.alpaca.markets

# Anthropic
ANTHROPIC_API_KEY=sk-ant-...

# Finnhub
FINNHUB_API_KEY=your_finnhub_api_key

# Email notifications (optional — set EMAIL_ENABLED=false to disable)
# Delivery uses Resend (https://resend.com) over HTTPS — DigitalOcean blocks SMTP.
# For the Resend sandbox, set EMAIL_SENDER=onboarding@resend.dev (sends only to
# the Resend account owner's email). For other senders, verify a domain first.
EMAIL_ENABLED=false
EMAIL_RECIPIENT=you@example.com
EMAIL_SENDER=onboarding@resend.dev
RESEND_API_KEY=re_your_resend_api_key

# Optional path overrides (defaults are shown)
# DB_PATH=tradingbot.db
# REPORTS_DIR=reports
# WATCHLIST_PATH=config/watchlist.yaml
